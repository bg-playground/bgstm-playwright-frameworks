---
"@bgstm/playwright-core": minor
---

Add a BGSTM Playwright reporter skeleton with session lifecycle reporting (`onBegin` create session, `onEnd` finish session), typed external-results payloads, and unit tests.

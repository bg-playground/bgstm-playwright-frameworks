{
  "name": "@bgstm/domain-accounting",
  "version": "0.0.0",
  "private": true,
  "description": "Accounting domain pack for Playwright — Ledgers, Journals, Reconciliations",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:e2e": "playwright test --project=e2e",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bgstm/playwright-core": "workspace:*"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.48.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.4"
  }
}

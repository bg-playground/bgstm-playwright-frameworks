{
  "name": "crm-example",
  "private": true,
  "version": "0.0.0",
  "description": "Minimal Playwright smoke suite (1 pass / 1 fail / 1 skip) wired to BGSTMReporter",
  "scripts": {
    "test": "echo \"(intentional smoke fixture — run via 'pnpm smoke' or step-2 CI; skipped from 'pnpm -r run test' on purpose)\"",
    "smoke": "playwright test"
  },
  "dependencies": {
    "@bgstm/playwright-core": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}

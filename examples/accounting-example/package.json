{
  "name": "accounting-example",
  "private": true,
  "version": "0.0.0",
  "description": "Standalone runnable Playwright project using @bgstm/domain-accounting",
  "scripts": {
    "test": "echo \"(structural smoke fixture — run via 'pnpm smoke' or step-2 CI; skipped from 'pnpm -r run test' on purpose)\"",
    "smoke": "playwright test"
  },
  "dependencies": {
    "@bgstm/playwright-core": "workspace:*",
    "@bgstm/domain-accounting": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
